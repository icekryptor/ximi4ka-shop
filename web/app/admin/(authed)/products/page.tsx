import { AdminPlaceholder } from '@/components/admin/AdminPlaceholder'

export default function AdminProductsPage() {
  return (
    <AdminPlaceholder
      title="Товары"
      todo="Task 3.3 — CRUD-список товаров с фильтрами, редактором и превью блоков."
    />
  )
}
