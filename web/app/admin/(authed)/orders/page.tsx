import { AdminPlaceholder } from '@/components/admin/AdminPlaceholder'

export default function AdminOrdersPage() {
  return (
    <AdminPlaceholder
      title="Заказы"
      todo="Task 3.6 — список и детальная страница заказа с переходами статусов."
    />
  )
}
