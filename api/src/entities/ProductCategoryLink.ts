// The product_category_links junction table is managed implicitly by TypeORM
// via the @JoinTable decorator on ProductCategory.products. No explicit entity
// class is needed. If a richer join-table model is required later (e.g. per-link
// metadata like "featured order"), convert this file to a full entity.
export {}
